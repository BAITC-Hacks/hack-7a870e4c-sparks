# Линт и автофикс только по измененным файлам (Biome)
pnpm exec lint-staged

# Быстрая проверка архитектурных границ всего проекта
pnpm run lint:deps

# Неиспользуемые файлы, экспорты и зависимости (Knip)
pnpm run lint:unused
